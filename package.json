{
  "title": "RazorJS",
  "name": "@andyedinborough/razor-js",
  "description": "A JavaScript implementation of the Razor view engine",
  "version": "0.4.3",
  "author": {
    "name": "Andy Edinborough (@andyedinborough)"
  },
  "repository": "https://github.com/andyedinborough/RazorJS",
  "licenses": [
    {
      "type": "MIT"
    }
  ],
  "devDependencies": {
    "@types/bun": "^1.3.4",
    "typescript": "5.9.3"
  },
  "scripts": {
    "compile": "./node_modules/.bin/tsc",
    "build": "rm -rf ./dist/ && bun lint && bun test && bun compile",
    "lint": "eslint . --ext .ts --fix",
    "x-publish": "bun run build && cp ./package.json ./dist/package.json && npm publish ./dist --access=public"
  }
}
